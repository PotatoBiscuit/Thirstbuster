package com.CS477.drinkandgo;

import android.content.Context;
import android.widget.RelativeLayout;
import android.widget.TextView;

import com.CS477.drinkandgo.activies.DrinkAndGoActivity;

public class VenueLayout extends RelativeLayout 
{	
	private final LayoutParams params;
	
	public VenueLayout(Context context, Venue venue)
	{	this(context, venue, DrinkAndGoActivity.makeDefaultParams());}
	
	public VenueLayout(Context context, Venue venue, LayoutParams params) 
	{
		super(context);
		
		this.params = params;
		
		TextView name = new TextView(context);
		name.setText(venue.getName());
		name.setId(1);
		
		TextView desc = new TextView(context);
		desc.setText(venue.getAddress());
		desc.setId(2);	
		
		params.addRule(ALIGN_PARENT_TOP);
		addView(name, params);
		
		params.addRule(ALIGN_PARENT_TOP, 0);
		params.addRule(BELOW, 1);
		addView(desc, params);
	}

	public LayoutParams getParams() 
	{	return params;}
}
